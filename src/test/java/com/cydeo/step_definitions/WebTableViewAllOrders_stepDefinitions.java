package com.cydeo.step_definitions;

public class WebTableViewAllOrders_stepDefinitions {

    /*
    WebTableLoginPage loginPage = new WebTableLoginPage();
    WebTableOrdersPage ordersPage = new WebTableOrdersPage();
    @Given("user is already logged in to The Web table app")
    public void user_is_already_logged_in_to_the_web_table_app() {
        Driver.getDriver().get("https://web-table-2.cydeo.com/login");
        loginPage.login();
    }
    @When("user is on the Order page")
    public void user_is_on_the_page() {
        Assert.assertEquals("Cydeo Web Table App",Driver.getDriver().getTitle());
    }
    @Then("user sees below options under product dropdown")
    public void user_sees_below_options_under_product_dropdown(List<String> products) {

        String eachProduct = "";
        for (String each : products) {
            eachProduct = each;
        }
        Assert.assertTrue(ordersPage.productsList().contains(eachProduct));
        System.out.println(products);
        System.out.println(ordersPage.productsList());
    }

     */
}
